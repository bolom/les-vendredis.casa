<h1>Nouvelle page de contenu</h1>
<p class="admin-definition">Le chemin devient l’URL publique : <code>/&lt;chemin&gt;</code> en anglais, <code>/fr/&lt;chemin&gt;</code> en français (préfixer alors le chemin de <code>fr/</code>).</p>

<%= render "form" %>
